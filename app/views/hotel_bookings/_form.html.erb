<%= form_with(model: hotel_booking) do |form| %>
  <% if hotel_booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hotel_booking.errors.count, "error") %> prohibited this hotel_booking from being saved:</h2>

      <ul>
        <% hotel_booking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :phoneno %>
    <%= form.number_field :phoneno %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
